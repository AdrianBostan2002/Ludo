import { RoleType } from "../enums/roletype.enum";

export class User {
    username!: string;
    role!: RoleType;

    // constructor(username: string, role: RoleType) {
    //     this.username = username;
    //     this.role = role;
    // }
}